# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables - NEVER commit these!
.env
.env.local
.env.development
.env.production
.env.test
backend/.env
backend/.env.local
backend/.env.development
backend/.env.production
frontend/.env
frontend/.env.local
frontend/.env.development
frontend/.env.production

# Test files with credentials
test-mongodb*
test-*.js

# User uploads — never commit (may contain sensitive resumes/photos)
uploads/

# QA test artifacts — generated per run
tests/results/

# Playwright reports and test results — heavy binaries (videos, traces, screenshots)
frontend/playwright-report/
frontend/playwright-exploration-report/
frontend/playwright-overnight-report/
frontend/playwright-walker-report/
frontend/playwright-walker-report-desktop/
frontend/playwright-walker-report-pixel5/
frontend/playwright-walker-report-iphone12/
frontend/test-results/
frontend/test-results-phase-14/
frontend/walker-screenshots/

# Local-only directories
backups/
instagram-launch/
backend/backend/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code: local agent state, plans, memory, settings
# These can contain session notes, internal infrastructure references,
# and project history that should never end up in a public repo.
.claude/

# Generated coverage reports (Phase 28)
backend/coverage/
coverage/
